#include <stdio.h>

int main() {
int a,b ;
printf(" Enter a :");
scanf("%d",&a);
printf(" Enter b :");
scanf("%d",&b);
if(a==b){printf("%d Two integers are equal.");
}
else{printf("%d Two integers are not equal.");}
return 0;
}
